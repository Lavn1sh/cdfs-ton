# Build outputs
bin/
build/
*.o

# Runtime data (generated by the servers)
edits.log
fsimage.dat
storage_data/

# Leftover chunk files at project root (from old storage.c behaviour)
chunk_*.dat
chunk_*.crc

# Config files (Ignore custom configs, keep default)
*.conf
!cdfs.conf
